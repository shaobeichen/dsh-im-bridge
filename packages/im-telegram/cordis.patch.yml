# dsh-im-telegram bundle patch（dsh.bundle manifest 要求的加载配置）
# 安装：dsh plugin --profile web add dsh-im-telegram
# 渠道适配器需要同时安装核心：dsh plugin --profile web add dsh-im dsh-im-telegram
- insert:
    - id: im-telegram
      name: dsh-im-telegram
